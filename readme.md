# Patient portal comparison app

## Requirements
- Java 8+
- Maven
- Docker
- Node & NPM


## Running
- git clone
- git submodule init && git submodule update
- build each sub-folder according to instructions in them
- docker-compose up (optional: service name)
